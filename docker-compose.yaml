version: '3'
services:
    web:
        restart: always
        build:
            context: .
        ports:
          - "4000:3000"
        volumes:
            - /usr/app/node_modules
            - .:/usr/app
